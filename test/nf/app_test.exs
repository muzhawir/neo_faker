defmodule Nf.AppTest do
  use ExUnit.Case, async: true

  # describe "author/0" do
  #   test "returns the app author" do
  #     assert Nf.App.author() == "John Doe"
  #   end
  # end

  # describe "name/0" do
  #   test "returns the app name" do
  #     assert Nf.App.name() == "NeoFaker"
  #   end
  # end

  # describe "semantic_version/0" do
  #   test "returns the app semantic version" do
  #     semver = Nf.App.semver()
  #     assert Nf.App.semantic_version() == "1.2.34"
  #   end
  # end

  # describe "version/0" do
  #   test "returns the app version" do
  #     assert Nf.App.version() == "0.3.1"
  #   end
  # end

  # describe "license/0" do
  #   test "returns the app license" do
  #     assert Nf.App.license() == "MIT"
  #   end
  # end
end
